# Зависимости для тестирования VPN Sales System
# Установка: pip install -r requirements-test.txt

pytest>=7.4.0
pytest-asyncio>=0.23.0
httpx>=0.27.0

# aiosqlite 0.21.0 совместим с Python <= 3.13
# Для Python 3.14+ используйте PostgreSQL для тестов или понизьте версию Python
aiosqlite==0.21.0

# Для Windows + Python 3.14 рекомендуется использовать:
# - Docker с PostgreSQL для интеграционных тестов
# - pytest-mock для unit тестов без реальной БД
pytest-mock>=3.12.0
